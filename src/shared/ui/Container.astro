---
type Props = {
	className?: string;
	children?: any;
};

const { className, children } = Astro.props;
---

<!-- Переписать классы если нужно -->
<div class={`mx-auto flex justify-center px-4 md:px-8 lg:px-12 items-center w-full max-w-[1400px] ${className}`}>
	<slot />
</div>
